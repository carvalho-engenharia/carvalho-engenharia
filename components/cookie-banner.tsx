"use client"

// Banner de cookies desativado a pedido do cliente.
// O componente é mantido no projeto (caso queira reativar no futuro),
// mas não renderiza mais nada, então não conflita com o botão do WhatsApp.
export function CookieBanner() {
  return null
}
